<local:PipelineControl x:Class="ABPipeline.Components.WriterPad"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:c="clr-namespace:Albert.Win32.Controls;assembly=Albert.Win32"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ABPipeline.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition />
			<RowDefinition Height="auto" />
		</Grid.RowDefinitions>
            <!-- ToolBar Tray-->
		<ToolBarTray DataContext="{StaticResource presetViewModel}">
			<ToolBar>
				<ComboBox x:Name="styleComboBox" ItemsSource="{Binding NoteStylePrests}" ItemTemplate="{StaticResource noteStyle}" Width="140" />
				<ComboBox ToolTip="Select Font Family" x:Name="fontComboBox" ItemTemplate="{StaticResource basicList}" Width="140" ItemsSource="{Binding NoteFontPresets}" />
				<Slider x:Name="fontSizeSlider" Value="18" Minimum="15" Maximum="150" ToolTip="Choose the Font Size" />
			</ToolBar>
		</ToolBarTray>
		<!-- WriterBox -->
		<c:WriterBox x:Name="writerBox" Grid.Row="1" />
		<!-- Add to Notes Button -->
		<c:PushButton Content="+" Grid.Row="2" ToolTip="Add Notes" />
	</Grid>
</local:PipelineControl>
